export class FeedItem {
   
    feedItem:string;
    name:string;
    photoURL:string;
    donationType:string;
    date:string;
    completed:boolean; // whether doner found or not
    //should have same name as database columns
    
}